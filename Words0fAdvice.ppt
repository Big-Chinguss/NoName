1. In problems having you to make a pair as such (a[i]+a[j]) should be closest to x, Always use abs() for taking distance b/w x and a[i]+a[j].
2. While using prefix sum in array, initallise array with 0, and start inputing from 1 to n.
